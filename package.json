{
  "name": "bvm-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend/BVM\" \"nodemon backend/server.js\" \"uvicorn RAG.main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.10"
  }
}
